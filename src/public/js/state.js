// 共有状態（全モジュールから参照・更新）
export const state = {
  activeTab: "dashboard",
  activeUserId: "sensei_default",
  userProfiles: ["sensei_default"],
  pendingTasksCount: 0,
  totalExpensesVal: 0,
  isAdmin: false,
};
